export interface AuthModel {
  access_token: string
  refreshToken?: string
  }

  export interface UserModel {
    idSubscriber: string;
    username: string;
    firstName: string;
    lastName: string;

    role: string;
    
   
  }
  